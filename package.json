{
  "name": "cs",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "surge": "^0.18.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified.\"",
    "dev": "live-server --port=3000 --no-browser"
  }
}
